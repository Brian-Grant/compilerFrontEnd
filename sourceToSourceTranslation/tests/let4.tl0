
#nested let's
fn (h a b c)  # 3 4 5
  (let
     a (let
          x * a b     # 12
	  b * + a b c # 35
	in
	  % + x b c   # 2
       )
     b 22
   in
     -
       + a b   #24
       (let
          a + a c # 7
          b 3     # 3
        in
          + - a b c # 7 - 3 + 5
        ) # 9
  ) #24 - 9
;
(h 3 4 5);  #15 
     
