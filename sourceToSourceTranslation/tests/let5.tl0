#nested let's

fn (h a b c)  
  (let
     a (let
        x * a b     
	    b * + a b c 
	  in
	    + % + x b c + a c  
       )
     b 22
   in
     -
       + a b   
       (let
          a + a c 
          b 3     
        in
          + - a b c 
        ) 
  ) 
;
(h 3 4 5);  #15 
#CHANGE POINT
